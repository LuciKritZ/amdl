# Stub Dockerfile for CI and validation when the real wrapper is not yet extracted.
# Module 3 (install-wrapper) replaces this directory with the real wrapper release.
FROM debian:stable-slim
EXPOSE 10020 20020 30020
CMD ["sleep", "infinity"]
