[project]
name = "amdl"
version = "0.1.0"
description = "Deterministic Apple Music → ALAC ingestion"
requires-python = ">=3.10"

dependencies = [
  "requests>=2.31.0",
  "python-dotenv>=1.0.0",
]

[dependency-groups]
dev = [
  "pytest>=7.0.0",
]
